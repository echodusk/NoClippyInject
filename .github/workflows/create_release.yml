name: Create Release and Update Repository

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    uses: UnknownX7/DalamudPluginRepo/.github/workflows/reusable_create_release.yml@master
    with:
      internal_name: NoClippyInject
      solution_name: NoClippyInject
      build_configuration: Release
      personal_plugin_repo: echodusk/NoClippyInject
      personal_plugin_repo_branch: master
    secrets: inherit
